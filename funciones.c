#include <stdio.h>
// declara una funcion sin inicializarla
int j(void);

// intercambia valores, para ordenar
void swap(int a, int b)
void swap(int array[], int i, int j);

void to_lower(char *s);

// funcion "pura" (no depende del entorno fuera de la funcion)
int suma(int a, int b) {
  return a + b:
}


// llama a la funcion
suma(3,3);
suma(3,3);
suma(3,3);
suma(3,3);
suma(3,3);
suma(3,3);
// no anda porque el tipo de dato que se devolveria es difernte al tipo de dato de la funcion
//funcion "impura", lo que se devuelve depende de la variable cavalcanti
int f(void){
  printf("%s\n", "cavalcanti");//side effect
  return "hola";
}

int h(void){

}

// el programa siempre empieza por main, aunque este en lo mas bajo del codigo
int main(void) {

}
